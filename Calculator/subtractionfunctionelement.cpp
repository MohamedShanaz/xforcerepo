#include "subtractionfunctionelement.h"

SubtractionFunctionElement::SubtractionFunctionElement()
{

}

SubtractionFunctionElement::~SubtractionFunctionElement()
{

}

double SubtractionFunctionElement::evaluate(vector<token> tokens){

    double answer=0.0;
 /*   vector<double> a(20,0);
    for(unsigned int i=0;i<tokens.size();i++)
       {

          if(tokens[i].number){
              std::ostringstream ss;
              ss << tokens[i].f;
              std::string s(ss.str());
              a.assign(i)=tokens[i].f;
             // answer=tokens[i].f - answer;
          }

    }

 //ScientificCalc obj;


// obj.value=answer;


    for(int i=0;a.size();++i)
 answer= a.data(i)-answer;
*/
  return answer;
}
